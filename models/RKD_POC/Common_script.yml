models:
  - name: tran_digital_adsales_actuals_manual
    description: '{{doc("TRAN_DIGITIAL_ADSALES_ACTUALS")}}'
    config:
        src_name: "Digital_Adsales_Actuals"
        stage: "Bronze"
        ops_upd: "UPD"
        ops_ins: "INS"
        status_success: "Success"
        status_start: "Started"
        status_fail: "Failed"
        proc_typ_msg_start: "Started validation checks for Transient Table"
        proc_typ_msg_success: "validation checks for Transient Table are successfull"
        integration_id: ''
        flag: 'Y'
    
    columns:
          - name: BUSINESS_IDENTIFIER
            description: '{{doc("DG_BUSINESS_IDENTIFIER")}}'
            tests:
              - null_check
          - name: BRAND
            description: '{{doc("DG_LEGACY_BRAND")}}'
            tests:
              - null_check
          - name: MEMO_ACCOUNT 
            description: '{{doc("DG_MEMO_ACCOUNT")}}'  
            tests:
              - null_check          
          - name: PLATFORM 
            description: '{{doc("DG_LEGACY_PLATFORM")}}'
            tests:
              - null_check
          - name: COMPANY_CODE
            description: '{{doc("DG_LEGACY_COMPANY_CODE")}}'
            tests:
              - null_check
          - name: GEOGRAPHY
            description: '{{doc("DG_LEGACY_GEOGRAPHY")}}'
            tests:
              - null_check
          - name: WEEKS
            description: '{{doc("DG_WEEKS")}}'
            tests:
              - null_check
          - name: TRADING_PARTNER
            description: '{{doc("DG_TRADING_PARTNER")}}'
            tests:
              - null_check
          - name: PERIOD
            description: '{{doc("DG_PERIOD")}}'
            tests:
              - null_check
          - name: YEAR
            description: '{{doc("DG_YEAR")}}'
            tests:
              - null_check
          - name: DATA_SOURCE
            description: '{{doc("DG_DATA_SOURCE")}}'
            tests:
              - null_check
          - name: CURRENCY
            description: '{{doc("DG_CURRENCY")}}'
            tests:
              - null_check
          - name: MARKET_TYPE
            description: '{{doc("DG_MARKET_TYPE")}}'
            tests:
              - null_check
          - name: SERVICE_PROVIDER
            description: '{{doc("DG_SERVICE_PROVIDER")}}'
            tests:
              - null_check
          - name: AMOUNT
            description: '{{doc("DG_AMOUNT")}}'
            tests:
              - null_check
          
            
          
            
          